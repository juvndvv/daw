const URL_START = 'http://localhost:8000/coop24.php';

const articulosVentaURL = URL_START + '?opcion=AV';
const socioArticulosURL = URL_START + '?opcion=SV';
const loginURL = URL_START + '?opcion=SR';
const registroURL = URL_START + '?opcion=RS';
const modificarSocioURL = URL_START + '?opcion=MS';
const existeEmailURL = URL_START + '?opcion=EE';
const categoriasURL = URL_START + '?opcion=TC';
const registroArticuloURL = URL_START + '?opcion=RA';
const borradoArticuloURL = URL_START + '?opcion=BA';
const articuloConsultaURL = URL_START + '?opcion=AC';
const modificarArticuloURL = URL_START + '?opcion=MA';
const comprarArticuloURL = URL_START + '?opcion=CA';
const socioConstultaURL = URL_START + '?opcion=SC';
const consultaVentaURL = URL_START + '?opcion=VA';
const todosSociosURL = URL_START + '?opcion=TS';
const todasVentasURL = URL_START + '?opcion=TV';
const todosArticulosURL = URL_START + '?opcion=TA';
const bloquearSocioURL = URL_START + '?opcion=BS';
const crearCategoriaURL = URL_START + '?opcion=CC';
const eliminarCategoriaURL = URL_START + '?opcion=EC';
const modificarCategoruaURL = URL_START + '?opcion=MC';
const obtenerCategoriaURL = URL_START + '?opcion=OC';

export const env = {
  articulosVentaURL,
  loginURL,
  registroURL,
  socioArticulosURL,
  modificarSocioURL,
  existeEmailURL,
  categoriasURL,
  registroArticuloURL,
  borradoArticuloURL,
  articuloConsultaURL,
  modificarArticuloURL,
  comprarArticuloURL,
  socioConstultaURL,
  consultaVentaURL,
  todosSociosURL,
  todasVentasURL,
  todosArticulosURL,
  bloquearSocioURL,
  crearCategoriaURL,
  eliminarCategoriaURL,
  modificarCategoruaURL,
  obtenerCategoriaURL,
};
